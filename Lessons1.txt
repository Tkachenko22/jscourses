1.Создайте переменную “fruit” и запишите в неё значение “baNaNa” не используя букву N. Используйте обратные кавычки для необходимых вычислений

let fruit = `ba${2/'a'}a`;
console.log(fruit);

2. Создайте переменную breakfest и скопируйте в неё значение переменной “fruit” !!!!

let fruit = `ba${2/'a'+'a'}`;
console.log(fruit);
let breakfest = fruit;
console.log(breakfest);

3. Задайте несколько констант в одной инструкции. Имена констант придумайте любые, значения число “3.14” и строка “USD”

{
  const myCurrency = "USD", myMoney = 3.14;

}

4.Создайте две переменных с любыми именами. Присвойте им любые числовые значения. В третью переменную запишите результат возведения одной переменной в степень другой. Для возведения в степень используйте оператор мультиплекации  **.
 
 
 let a=3;
 let b=5;
 let c= a**b;
 console.log(c);

5.Создайте переменную с именем из двух слов и запишите в неё значение “Hello world!”. !!!

let myOption = "Hello world";
console.log(myOption);










